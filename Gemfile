source 'https://rubygems.org'
# Allow any Jekyll 4.x version (including 4.4.x used by some build environments)
gem 'jekyll', '>= 4.3.2', '< 5.0'

# Include the Modernist theme so Cloudflare Pages can install it during build
gem 'jekyll-theme-modernist'
